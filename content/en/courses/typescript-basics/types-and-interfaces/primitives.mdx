---
title: "Primitive Types"
order: 2
---

# Primitive Types

TypeScript supports the same primitives as JavaScript, with explicit type annotations.

- `string`, `number`, and `boolean` are the most common.
- Use `: type` after a variable name to annotate.
- TypeScript can often infer types from initial values.
