---
title: "Type Guards"
order: 2
---

# Type Guards

Type guards narrow types inside conditional blocks so TypeScript can infer the correct type.

- `typeof` and `instanceof` are built-in guards.
- Custom guards use the `is` keyword.
- Guards make runtime checks work with the type system.
