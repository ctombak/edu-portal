---
title: "Utility Types"
order: 3
---

# Utility Types

TypeScript provides built-in utility types to transform existing types.

- `Partial<T>`, `Required<T>`, `Readonly<T>`.
- `Pick<T, K>` and `Omit<T, K>` for subsets.
- `Record<K, V>` for mapping keys to values.
